# Agent Principles Lab · 统一站点规范

## 目标

将 Agent Loop、Agent 协作与 RAG 三套互动实验合并为同一学习站点。用户在顶部切换原理，不离开页面、不面对重复导航。

## 结构

1. 统一品牌导航：说明这是 Agent 原理学习站点。
2. 原理切换器：始终展示三个入口及其学习范围。
3. 动态标题：切换时同步更新当前学习目标和一句解释。
4. 原理模块：保留原模块的全部交互、动画、逐步控制和重置能力。

## 切换规则

- 默认打开 Agent Loop。
- 使用原生按钮和 `role="tab"`，选中状态同时具备背景、文字和 `aria-selected`。
- URL Hash 保存当前原理，刷新后能够恢复选择。
- 模块在统一站点内进入嵌入模式，隐藏重复导航和外层阴影。
- 子模块通过 `postMessage` 报告实际内容高度，外层同步调整，避免双重滚动条。
- 每次切换重新加载模块，确保实验从干净状态开始。
- 统一站点沿用各模块的慢速教学节奏：Agent Loop 每阶段约 10 秒；协作架构根据当前动画长度动态衔接，约 3.3–5 秒，避免完成后继续空等。
- RAG 以一个总标题承载“在线流程 / 离线流程”二级切换。在线流程使用唯一的“公司差旅报销标准”案例，以真实对话框承接输入与最终回答，依次演示接收问题、问题向量化、Query 改写增强、向量检索、Rerank、上下文组装与带引用生成。离线流程演示原始数据采集、文档解析、数据清洗、文本分割、文本向量化、向量入库与索引构建。
- RAG 的跨区域动作先高亮、再以约 6.2 秒完成移动；自动播放会等待动作完成，支持暂停、上一步、下一步和整项重置。
- 在线 Rerank 提供默认 `K = 3` 的 Top K 步进配置，范围为 1–5；调整后，候选 Chunk 的选中态、Context Window 内证据数量和最终回答引用同步变化。
- Query 改写必须同时展示原始 Query、完整增强 Query 以及增强动作标签，避免只用关键词胶囊代替最终检索语句。
- 离线文档解析用“多格式 → 对应提取方式 → 统一结构化文本”表达：扫描 PDF 与图片走 OCR，纯文本与 Word 直接提取，结果统一保留标题、章节、正文及来源元数据。
- 文本分割使用真实段落实例展示切分前后对应关系；向量数据库使用三维语义坐标空间展示多个 Chunk 的位置与语义聚类，而不是平面柱状图。
- 每次进入或切换原理时，当前原理模块内部显示同一套聚光灯引导：页面其余区域短暂压暗，大号指针与高亮窗口从实验中央移动到该模块真正的起点；到达后以定位圆环持续提示目标。Agent Loop 与 RAG 指向发送按钮，Agent 协作指向自动播放按钮。引导在 iframe 内部运行，外层仅通过消息触发重播，以兼容本地 `file://` 浏览器对 iframe 的访问隔离。
- 引导提供明确的跳过按钮；点击目标即自动消失。系统开启“减少动态效果”时不播放飞行动画，直接显示在最终提示位置。

## RAG 双流程教学模型

### 在线问答七步

接收问题 → 问题向量化 → Query 改写增强 → 向量检索 → Rerank → 组装上下文 → 生成带引用回答。

### 离线建库六步

原始数据采集 → 文档解析 → 数据清洗（去冗余、改写、统一格式、敏感信息过滤）→ 文本分割 → 文本向量化 → 写入向量数据库并建立向量索引。

## 响应式

- 桌面：三个原理入口并排，页面整体保持 90% 视觉比例。
- 移动端：入口上下排列，保留完整标题与说明；模块继续使用自身移动端布局。
