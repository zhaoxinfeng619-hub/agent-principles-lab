# Agent Collaboration Lab · 设计规范

## 1. 教学目标

用同一个真实案例解释两种不同的 Agent 协作结构，而不是只罗列名词：

- **Subagent**：一个主 Agent 拆解目标、向下委派、回收摘要并负责最终输出。
- **Multi-agent**：多个 Agent 围绕共享目标和共享状态协作，允许点对点反馈与交叉校验。

页面不展示模型私有思维链，只展示用户可以观察和验证的任务、消息、状态、证据与决策摘要。

## 2. 信息结构

1. 顶部：页面身份、实验时长、返回 Agent Loop。
2. 紧凑说明：一句问题说明本页要解决的认知目标。
3. 架构切换：Subagent / Multi-agent 两种模式。
4. 五阶段轨道：用一致节奏解释两种模式。
5. 动态关系图：节点、方向连线、消息包和当前高亮。
6. 当前动作：同步解释此刻发生什么以及为什么。
7. 底部对比：明确两种架构的责任归属和适用场景。

## 3. 图形语义

- 紫色节点：主控或协调角色。
- 青色节点：执行任务的专业 Agent。
- 荧光黄节点：共享状态、任务板或回传的信息。
- 绿色节点：已经满足完成条件的最终输出。
- 实线：稳定的通信通道。
- 动态虚线：当前正在发生的信息流。
- 移动消息包：委派、回传、反馈或共识，不做无语义的装饰运动。

## 4. 交互规范

- 默认进入 Subagent 模式，但首屏必须同时看见 Multi-agent 切换入口。
- 支持自动播放、暂停、上一步、下一步与重置。
- 点击任意阶段可以直接查看对应状态。
- 切换架构时重置进度，避免把两种模型的状态混在一起。
- 每一步同时更新节点、连线、移动消息、解释文字和事件记录。
- 信息包移动、节点高亮和并行任务错峰统一放慢到上一版的 2.5 倍；单条信息移动约 3.75 秒。自动播放不使用固定空等时间，而是按各阶段最长动画动态等待约 3.3–5 秒，消息抵达后约 300ms 进入下一步。
- Subagent 的最终输出线必须从主 Agent 沿独立底部通道直达最终方案，不得穿过任何专业 Subagent，避免暗示最终输出仍需经过某一个子角色。
- “拆解任务”阶段必须把完整任务可视化裂变为用户研究、指标测算和内容方案三张任务单，承接第一步输入动效。
- `prefers-reduced-motion` 下关闭移动消息与循环动画，保留节点和连线的最终高亮。

## 5. 响应式与无障碍

- 桌面使用“关系图 + 解释栏”的双栏结构。
- 1050px 以下改为上下结构；720px 以下把五阶段轨道改为两列，并把节点图改为纵向布局。
- 所有按钮使用原生语义，具备可见焦点；动态图状态通过 `aria-live="polite"` 播报。
- 颜色不单独承担含义，节点均有角色名、英文类型和职责描述。
