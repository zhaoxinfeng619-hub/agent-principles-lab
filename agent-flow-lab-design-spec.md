# Agent Flow Lab · UX/UI 设计规范 v2.0

## 1. 核心体验

页面不是一张“会动的流程图”，而是一段可亲自参与的对话。用户先像使用真实 AI 产品一样发送任务，再通过同步出现的内部视图理解 Agent 为什么这么做。

体验顺序固定为：**提出问题 → 看见 Agent 行动 → 在关键时刻解释原理 → 获得答案 → 回顾完整闭环**。

## 2. 体验原则

| 原则 | 具体规则 |
| --- | --- |
| 先体验，后解释 | 首屏主操作是对话输入，架构术语只在对应事件发生时出现。 |
| 一次只讲一件事 | 内部视图只突出当前阶段；解释卡同步更新，不同时展示大量日志。 |
| 失败也要有意义 | 工具失败不显示成报错终点，而是可理解的“证据不足”，随后演示 Agent Loop 如何修正。 |
| 进度可掌控 | 自动演示可暂停、继续或重放；提供逐步模式；动效偏好为减少时不自动播放。 |
| 技术真实但不过载 | 展示 Context、Planning、Tool Call、Agent Loop、Memory 等真实概念，但隐藏不可解释的内部思维过程，只展示可观察的步骤摘要。 |

## 3. 信息架构

1. **轻量导航**：品牌、学习进度、重放。
2. **顶部实验标识**：把“3 分钟互动实验”放在导航中，不额外占用首屏纵向空间。
3. **原理地图**：页面进入后直接呈现六个高可读步骤，运行时逐个点亮，完成后可点击回看。
4. **体验工作台**：左侧真实对话界面，右侧 Agent 内部视图；天气案例直接预填在对话输入框中。

移动端顺序调整为“顶部实验标识 → 两列原理步骤 → 对话 → 当前原理”，不强迫用户横向理解复杂流程图。

## 4. 视觉系统

### 4.1 风格

采用“**友好型编辑部 × AI 实验室**”：明亮暖白背景、深墨色文字、紫色主品牌、青色操作反馈、荧光黄作为教学提示。使用清晰边框和轻微错位阴影制造有趣感，不做厚重控制台。

### 4.2 色彩

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--canvas` | `#F8F5FF` | 页面背景 |
| `--paper` | `#FFFFFF` | 主卡片、对话表面 |
| `--ink` | `#211D3A` | 主文字 |
| `--muted` | `#625D78` | 次要说明 |
| `--purple` | `#6D46D9` | 品牌、主要动作、LLM |
| `--cyan` | `#087F95` | 工具、交互反馈 |
| `--lime` | `#DDFC76` | 当前教学提示 |
| `--coral` | `#FF8066` | 证据不足、需要循环 |
| `--border` | `#D8D0EA` | 边界与分隔 |

所有状态同时具备图形、文字和色彩，不依赖颜色单独传达。

### 4.3 字体

- 标题：Space Grotesk，强调结构和亲和力。
- 正文：DM Sans，16px 起，行高 1.55–1.7。
- Token、API 名与计数：系统等宽字体，仅用于短标签。

### 4.4 几何与空间

- 基础间距：4 / 8 / 12 / 16 / 24 / 32 / 48。
- 面板圆角：22–28px；控件圆角：12–16px；状态胶囊：999px。
- 主边框：1px；重点卡边框：2px；错位阴影只用于最重要的工作台和教学卡。
- 最大内容宽度：1440px；主工作台桌面为 58:42 双栏。
- 1101px 以上桌面视口对页面整体使用 90% 等比缩放，保持字体、卡片、边框与动效比例一致，让完整工作台更稳定地进入首屏；平板和移动端保持 100% 尺寸。

## 5. 对话界面规范

- 用户消息靠右，使用紫色气泡；Agent 消息靠左并带角色头像。
- 输入框拥有可见标签、示例提示、`Enter` 发送和 `Shift + Enter` 换行说明。
- 发送后按钮进入明确的运行状态，避免重复提交；用户可随时点击“停止”。
- 首次进入时，在输入区上方显示非阻塞式引导卡和指向发送键的箭头；用户聚焦输入框、输入内容或开始任务后立即隐藏，不反复打扰。
- Agent 的执行过程不是假装打字的长文本，而是一张可展开的“正在行动”卡：显示当前阶段、简短原因、工具结果和回路次数。
- 最终回答使用真正的对话消息呈现，同时提供“查看依据”按钮展开工具证据。

## 6. 原理视图规范

- 使用六个可点击阶段：装入上下文 → 理解任务 → 制定计划 → 工具调用 → 生成回答 → 写入记忆。英文术语作为辅助标签，不再作为主要标题。
- 当前阶段使用紫色实心节点与小幅呼吸；已完成阶段显示勾选；未开始阶段保持低对比。逐步模式同时提供“上一步”和“下一步”，回退时恢复该阶段的场景、解释、统计与执行状态。
- Agent Loop 通过从“工具调用”返回“装入上下文”的回流带呈现，只有真实触发时才展开，避免首屏过早增加复杂度。
- 进入 Agent Loop 时，原理地图同时出现动态回流箭头、圈住前四个节点的循环框，以及高对比的 `AGENT LOOP · 循环执行中` 标签；线、区域、文字三层信息共同说明“工具结果回到上下文，再规划一轮”。
- 关键节点名称使用更大的中文主标题和更高字重；英文术语仅作辅助识别，当前节点标签使用高对比底色。
- 解释卡始终回答三个问题：**现在发生什么、为什么需要它、带来了什么结果**。
- Context Window 用组合条显示 System / User / Tool Result，而非只显示抽象 token 数。
- 工具调用以天气图标、温度、降水、风力和日落时间为主视觉，API 代码只作为次级摘要。
- 生成回答阶段必须直接显示推荐时段、判断依据和结论预览，不使用没有语义的占位线。

## 7. 动效规范

- 自动演示的所有阶段统一每 10 秒推进一次，为放慢后的信息移动和阅读留出完整时间。
- 用户发送任务后，问题气泡跨越左右面板进入 Context Window，明确表现“用户输入被装入上下文”。
- 每次跨区域移动前，源信息先进行约 2.75 秒的双次高亮闪烁，并显示“即将送入上下文”或“即将回流”；确认信息来源后，再执行约 5.75 秒的空间移动，整体速度为上一版的 40%。
- Planning 先保留完整用户目标，再按顺序裂变出三个可执行子任务，而不是一次性显示静态列表。
- 工具信息不足时，天气结果卡沿 Agent Loop 回流到 Context Window，并触发 Context 接收反馈。
- 规划拆解和上下文接收等局部教学反馈同步放慢到上一版的 2.5 倍；表达信息跨区域移动的完整关键动效使用约 8.5 秒。
- 所有自动播放都提供暂停；`prefers-reduced-motion` 下关闭位移、呼吸和自动播放，保留静态最终状态。
- 播放控制区始终提供“重置”操作，一键清除当前对话、阶段进度、统计、Agent Loop、最终回答和运行状态，并恢复默认案例输入。

## 8. 无障碍与响应式

- 正文/背景对比不低于 4.5:1；交互边界不低于 3:1。
- 所有可操作控件至少 44×44px，键盘焦点清晰，图标按钮拥有可访问名称。
- 动态状态通过一个 `aria-live="polite"` 区域播报，不抢夺焦点。
- 375px 下不产生横向滚动；顶部标识按空间收起辅助信息；对话输入保持 16px 防止移动端缩放。
- 失败信息明确说明原因和下一步，不只显示“出错了”。

## 9. 验收标准

- 任意输入内容都能成为对话中的用户消息，并启动完整演示。
- 页面只保留一个天气案例；默认路径触发两轮规划与一次工具证据不足。
- 用户可暂停、继续、逐步推进、回退、重置、重放和展开最终依据。
- 对话、内部节点、上下文构成、解释卡和学习进度始终同步。
- 页面首次打开 5 秒内能理解如何开始，不需要先阅读架构说明。
